/**
 * DEPRECATED — replaced by the unified global footer
 * (src/components/shell/Footer). No-op kept for legacy call sites.
 */
import React from 'react';

export default function SiteFooter(): React.ReactElement | null {
  return null;
}
